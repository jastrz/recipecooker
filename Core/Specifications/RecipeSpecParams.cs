namespace Core.Specifications
{
    #nullable enable
    public record RecipeSpecParams(
        string? MainIngredient,
        string? Origin,
        string? Character
    );
    #nullable disable
}